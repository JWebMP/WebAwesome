package com.jwebmp.webawesome.components.icon;

/**
 * Flip direction options for WaIcon.
 */
public enum IconFlip
{
    X,
    Y,
    Both;

    @Override
    public String toString()
    {
        return name().toLowerCase();
    }
}

